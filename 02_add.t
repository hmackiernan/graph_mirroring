# add a file and commit to partner system
# ensure that commit is mirrored to helix
# by syncing in a client mapping the repo

# in partner system, git client
# clone the partner system repo $partner_rep as $partner_user
# add a file, commit, push to partner system

# in helix using perforce client
# sync, test that add commit is mirrored
